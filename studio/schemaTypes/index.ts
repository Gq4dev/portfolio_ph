// Single source of truth: schema definitions live at the Astro root
// (../../src/sanity/schemaTypes) and are shared by BOTH the embedded
// Studio (/admin) and this standalone Studio. Do not duplicate them here.
export { schemaTypes } from '../../src/sanity/schemaTypes'
