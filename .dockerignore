# VCS / local
.git
.gitignore

# Dependencies & build output (reinstalled/rebuilt inside the image)
node_modules
dist
.astro
.sanity

# Secrets & env (public Sanity config is passed via build args)
.env
.env.*

# Heavy sample images (live in the CMS, not the image)
FOTOS

# Standalone studio is not used by the production build
studio

# Misc
npm-debug.log*
Thumbs.db
.DS_Store
