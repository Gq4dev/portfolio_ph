# Umami — self-hosted, privacy-first analytics.
# Deploy as a SEPARATE Portainer stack on the Hetzner box.
#
# 1. Set two env vars in the Portainer stack (Environment variables):
#      UMAMI_DB_PASSWORD  → any strong password
#      UMAMI_APP_SECRET   → any long random string (e.g. `openssl rand -hex 32`)
# 2. Point an A record for u.jesicamarianaph.com at the server BEFORE
#    deploying — Traefik needs it to resolve to pass the HTTP-01 challenge.
# 3. Deploy. Umami comes up at https://u.jesicamarianaph.com
# 4. Log in with the default admin / umami, then CHANGE that password.
# 5. Add a website ("Jesica portfolio"), copy its Website ID + the tracking
#    <script> src, and hand them over — they get wired into the site build via
#    PUBLIC_UMAMI_SRC and PUBLIC_UMAMI_ID.
services:
  umami:
    image: ghcr.io/umami-software/umami:postgresql-latest
    restart: unless-stopped
    depends_on:
      umami-db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://umami:${UMAMI_DB_PASSWORD}@umami-db:5432/umami
      DATABASE_TYPE: postgresql
      APP_SECRET: ${UMAMI_APP_SECRET}
    networks:
      - web
      - umami-internal
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      # Serve the real subdomain; keep the old nip.io host alive during the
      # switch so the tracking script keeps working until the site is rebuilt.
      - traefik.http.routers.umami.rule=Host(`u.jesicamarianaph.com`) || Host(`analytics.178.104.93.97.nip.io`)
      - traefik.http.routers.umami.entrypoints=websecure
      - traefik.http.routers.umami.tls=true
      - traefik.http.routers.umami.tls.certresolver=letsencrypt
      - traefik.http.services.umami.loadbalancer.server.port=3000

  umami-db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: umami
      POSTGRES_USER: umami
      POSTGRES_PASSWORD: ${UMAMI_DB_PASSWORD}
    volumes:
      - umami-db-data:/var/lib/postgresql/data
    networks:
      - umami-internal
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U umami -d umami']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  umami-db-data:

networks:
  web:
    external: true
  umami-internal:
